export default (children) => {
    return <>
        <header>
            Header
        </header>
        <main>
            {children}
        </main>
        <footer>
            footer
        </footer>
    </>
}